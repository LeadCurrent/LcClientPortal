@model Web.UserVM

@{
    if (Model.MobileApp)
        Layout = "~/Pages/Shared/NoNavLayout.cshtml";
    else
    Layout = "~/Pages/Shared/TemplateLayout.cshtml";
}

@using (Html.BeginForm("List", "User", FormMethod.Post, new { id = "form", enctype = "multipart/form-data" }))
{
    <div id="partialview">
        @{await Html.RenderPartialAsync("PartialViews/UserList_Partial.cshtml");}
    </div>
}
<script>
    highlightcurrentpage('nav_User');
</script>